require 'spec_helper'

describe Textlocal do
  it 'has a version number' do
    expect(Textlocal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
